import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ru.yandex.sdg.launcher',
  appName: 'audio-example',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
